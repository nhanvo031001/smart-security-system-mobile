import { Text } from "react-native";
import { View } from "react-native";

export default function VideoViewLivestream(props) {
    return (
        <View>
            <Text> live stream video </Text>
        </View>
    );
}
